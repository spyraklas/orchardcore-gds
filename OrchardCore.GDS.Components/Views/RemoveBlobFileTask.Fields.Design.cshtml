@model ActivityViewModel<RemoveBlobFileTask>

<header>
    <h4><i class="fa-solid fa-shield-alt" aria-hidden="true"></i>@Model.Activity.GetTitleOrDefault(() => T["Remove a blob file"])</h4>
</header>
<span>@T["Remove a file in the azure blob storage"]</span>
