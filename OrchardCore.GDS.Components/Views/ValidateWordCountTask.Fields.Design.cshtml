@model ActivityViewModel<ValidateWordCountTask>

<header>
    <h4><i class="fa-solid fa-shield-alt" aria-hidden="true"></i>@Model.Activity.GetTitleOrDefault(() => T["Validate Word Count For Form Field"])</h4>
</header>
<span>@T["Validate words count for <em>{0}</em>", Model.Activity.FieldName]</span>
