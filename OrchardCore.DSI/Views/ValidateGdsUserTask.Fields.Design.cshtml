
@model ActivityViewModel<ValidateGdsUserTask>

<header>
    <h4><i class="fa-solid fa-shield-alt" aria-hidden="true"></i>@Model.Activity.GetTitleOrDefault(() => T["Validate GDS User and Roles"])</h4>
</header>
<span>@T["Validate <em>{0}</em>", Model.Activity.UserRoles]</span>
